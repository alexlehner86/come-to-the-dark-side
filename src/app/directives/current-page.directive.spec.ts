import { CurrentPageDirective } from './current-page.directive';

describe('CurrentPageDirective', () => {
    it('should create an instance', () => {
        const directive = new CurrentPageDirective();
        expect(directive).toBeTruthy();
    });
});
